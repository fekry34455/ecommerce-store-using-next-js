/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      locales: ["ar"],
      defaultLocale: "ar",
      localeDetection: false,
    },
    reactStrictMode: true,
  };
  
  export default nextConfig;